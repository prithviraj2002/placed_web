import 'package:flutter/material.dart';

class ImportSheet extends StatelessWidget {
  const ImportSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Import a sheet'),),
    );
  }
}
